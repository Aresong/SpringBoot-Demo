server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server #eureka实例的主机名

  client:
    register-with-eureka: false #不把自己注册到eureka上
    fetch-registry: false #不从euraka上获取服务注册信息
    service-url:
      defaultzone: http://127.0.0.1:8761/eureka/